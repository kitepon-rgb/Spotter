# CodeGraph data files — local to each machine, not for committing.
# Ignore everything in .codegraph/ except the tracked configuration and this
# file itself, so transient databases, daemon state, sockets, and logs stay local.
*
!config.json
!.gitignore
